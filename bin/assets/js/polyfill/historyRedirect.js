/* History Redirect: */
/* Loaded if no support for history.js deeplinking is found - what it does is it takes the #! url and redirects it to a real url, to be handled by the HTML */
alert("lol, no history for you!!");